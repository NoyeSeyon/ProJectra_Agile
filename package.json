{
  "name": "projectra",
  "version": "1.0.0",
  "description": "Multi-tenant project management platform with Kanban, Slack integration, and AI assistant",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["mern", "project-management", "kanban", "multi-tenant", "slack", "ai"],
  "author": "Projectra Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
